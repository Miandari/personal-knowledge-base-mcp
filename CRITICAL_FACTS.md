# Critical facts — always loaded

- User: **mk**, PhD candidate in **computational social science**.
- Languages: **Python** primary, occasionally **R** and **Julia**.
- Daily habit: reads a Notion AI/ML dev briefing every day. Parent page `318a6df2-0ce4-80bf-ac67-e2a622e47636`.
- Primary interests: **agent memory**, **LLM context scaling**, **MCP**, **AI coding agents** (including critical takes), computational social science methods.
- Uses **both Obsidian.app and Claude Code** against this vault — preserve wikilinks, flat YAML frontmatter, and Obsidian-specific files.
- Retrieval: use the `kb` skill (`kb_explore` first, then `kb_search`). Never grep `wiki/**` directly.
- Compilation over retrieval: every ingest **rewrites** existing pages, not just appends new ones.
