{
  "mcpServers": {
    "pkb": {
      "command": "pkb",
      "args": ["--vault", "tests/fixtures/sample_vault", "server"],
      "env": {
        "KB_EMBEDDING_PROVIDER": "noop"
      }
    }
  }
}
